{
  "name": "@luciel/design-tokens",
  "version": "0.0.0",
  "private": true,
  "description": "The single source of truth for Luciel design tokens (Space Instructions §5). Consumed by every artifact; no artifact invents per-screen palettes.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint placeholder — eslint config added with first feature PR)\"",
    "test": "echo \"(no unit tests yet — tokens are data; contrast is verified by axe in apps)\""
  },
  "peerDependencies": {
    "tailwindcss": "^3.4.0"
  },
  "peerDependenciesMeta": {
    "tailwindcss": {
      "optional": true
    }
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10",
    "typescript": "^5.5.4"
  }
}
