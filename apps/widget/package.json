{
  "name": "@luciel/widget",
  "version": "0.0.0",
  "private": true,
  "description": "The embeddable chat widget. Vite library-mode \u2192 a single self-initializing bundle served from embed.vantagemind.ai/v1/luciel.js. Shadow-DOM isolated, tiny, framework-light, ZERO admin-API code (Space Instructions \u00a72, \u00a76.3).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/check-bundle.mjs",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(lint placeholder \u2014 eslint config added with first feature PR)\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@luciel/api-client": "workspace:*",
    "@luciel/design-tokens": "workspace:*",
    "@luciel/ui": "workspace:*"
  },
  "devDependencies": {
    "jsdom": "^24.1.1",
    "typescript": "^5.5.4",
    "vite": "^6.4.3",
    "vitest": "^3.2.6"
  }
}
