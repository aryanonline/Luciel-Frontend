# Keep the dependency tree strict so a package cannot accidentally import a
# dependency it did not declare. This protects the bundle-separation guarantee
# (Space Instructions §1): the widget bundle must never pull in admin-API code.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
