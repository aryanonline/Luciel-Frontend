{
  "_comment": "Waivers for `scripts/audit-gate.mjs` (pnpm audit, high+ blocks). Each entry needs a reason and an expiry; an expired entry fails the build. Rationale in audit-allowlist.md. Empty since the Next 14 → 15 migration (2026-09-07) retired the eight Next advisories; a new entry is a reviewed commit, never a quiet renewal.",
  "waivers": []
}
